@TestOn('vm')
library;

import 'package:test/test.dart';

void main() {
  group('genimg', () {
    test('convert', () async {
      try {
        //await runCmd(processCmd("convert", []), verbose: false);
      } catch (e) {
        print(e);
      }
    });

    //test()
  });
}
